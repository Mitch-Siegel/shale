#include "kprintf.sbh"
#include "console.sbh"
#include "kalloc.sbh"

// TODO: compile by number of cpus and change stack size
u8[4096] stack;

fun kstart()
{
    console_init();
    kputs("kstart!\n");
    kmeminit();
    kputs("bottom of kstart!\n");
}