
// TODO: u64 return 
u32 fun RISCV_readThreadPointer();

u32 fun RISCV_testAndSet(u32 *address, u32 newValue);

fun RISCV_lockRelease(u32 *address);

fun RISCV_synchronize();

fun RISCV_writeSatp(u64 newSatp);

u64 fun RISCV_readSie();

fun RISCV_writeSie(u64 newSie);

fun RISCV_writeMedeleg(u64 newMedeleg);

fun RISCV_writeMideleg(u64 newMideleg);

fun RISCV_mret();

u8 fun RISCV_intrEnabled();

fun RISCV_intrOff();

fun RISCV_intrOn();
